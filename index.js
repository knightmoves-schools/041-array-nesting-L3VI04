// Create an array named groupedNumbers
const groupedNumbers = [];

// Add an array containing 1, 3, and 5
groupedNumbers.push([1, 3, 5]);

// Add an array containing 2, 4, and 6
groupedNumbers.push([2, 4, 6]);

// Assign the innerHTML of the element with id "result" to groupedNumbers
document.getElementById('result').innerHTML = groupedNumbers;

